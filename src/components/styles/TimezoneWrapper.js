import styled from 'styled-components';

const TimezoneWrapper = styled.p`
  color: #a663be;
  font-size: 14px;
  font-weight: 500;
  text-align: center;
  overflow: hidden;
  text-overflow: ellipsis;  
`;

export default TimezoneWrapper;
