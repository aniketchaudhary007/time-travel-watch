import styled from 'styled-components';

const OuterWrapper = styled.div`
  background: rgba(255, 255, 255, 0.61);
  border-radius: 36px;
  box-shadow: 0px 20px 36px rgba(0, 0, 0, 0.13);
  height: 254px;
  padding: 20px;
  position: relative;
  width: 212px;
`;

export default OuterWrapper;
